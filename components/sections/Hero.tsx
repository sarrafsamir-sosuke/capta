"use client"

// Placeholder do Prompt 3. Vira "use client" desde já porque o Prompt 6 adiciona
// GSAP aqui. Conteúdo real entra no Prompt 4.
export function Hero() {
  return (
    <section id="hero" className="py-24">
      <p className="text-zinc-500">hero</p>
    </section>
  )
}
